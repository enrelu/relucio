---
---
<div class="h-full flex flex-col">
    <div class="mb-4">
        <h3 class="text-xl font-bold text-zinc-900 dark:text-zinc-100 mb-2">Info de Sistema</h3>
        <p class="text-sm text-zinc-600 dark:text-zinc-400">¿Qué información expone tu navegador?</p>
    </div>

    <div class="flex-grow space-y-4">
        
        <div class="p-4 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800 flex items-center justify-between">
            <div>
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Tu Dirección IP</p>
                <p id="ip-address" class="text-lg font-mono font-medium text-zinc-900 dark:text-zinc-100">Cargando...</p>
            </div>
            <div id="ip-indicator" class="w-3 h-3 rounded-full bg-zinc-300 animate-pulse"></div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Resolución</p>
                <p id="screen-res" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>
             
             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Idioma</p>
                <p id="browser-lang" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>

             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Plataforma</p>
                <p id="browser-platform" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>

             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Zona Horaria</p>
                <p id="browser-timezone" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>

             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">CPU / RAM</p>
                <p id="browser-hardware" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>

             <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800">
                <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">Cookies</p>
                <p id="browser-cookies" class="text-sm font-mono text-zinc-800 dark:text-zinc-200">...</p>
             </div>
        </div>

        <div class="p-3 bg-zinc-50 dark:bg-zinc-900/50 rounded-lg border border-zinc-200 dark:border-zinc-800 overflow-hidden">
            <p class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-1">User Agent</p>
            <p id="user-agent" class="text-xs font-mono text-zinc-600 dark:text-zinc-400 break-all leading-relaxed">...</p>
        </div>

        <div class="pt-2">
            <p class="text-xs text-zinc-400 italic">
                Nota: Esta información es visible para cualquier sitio web que visites. Usa una VPN para ocultar tu IP.
            </p>
        </div>
    </div>
</div>

<script>
    // IP Fetch
    fetch('https://api.ipify.org?format=json')
        .then(response => response.json())
        .then(data => {
            const ipEl = document.getElementById('ip-address');
            const indEl = document.getElementById('ip-indicator');
            if (ipEl && indEl) {
                ipEl.textContent = data.ip;
                indEl.classList.remove('bg-zinc-300', 'animate-pulse');
                indEl.classList.add('bg-green-500');
            }
        })
        .catch(err => {
            const ipEl = document.getElementById('ip-address');
            if (ipEl) ipEl.textContent = 'Bloqueado / Error';
        });

    // Browser Info
    document.getElementById('screen-res').textContent = `${window.screen.width} x ${window.screen.height} (Pixel Ratio: ${window.devicePixelRatio})`;
    document.getElementById('user-agent').textContent = navigator.userAgent;
    document.getElementById('browser-lang').textContent = navigator.language;
    document.getElementById('browser-platform').textContent = navigator.platform || 'Desconocido';
    document.getElementById('browser-timezone').textContent = Intl.DateTimeFormat().resolvedOptions().timeZone;
    document.getElementById('browser-cookies').textContent = navigator.cookieEnabled ? 'Habilitadas' : 'Deshabilitadas';
    
    // Hardware (Available in some browsers)
    const cores = navigator.hardwareConcurrency ? `${navigator.hardwareConcurrency} Núcleos` : '';
    const memory = navigator.deviceMemory ? `${navigator.deviceMemory} GB RAM aprox` : '';
    const hardwareInfo = [cores, memory].filter(Boolean).join(' / ') || 'No disponible';
    document.getElementById('browser-hardware').textContent = hardwareInfo;
</script>
